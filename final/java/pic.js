import java.util.Random;

function pic() {
    var random1 = 1 + (Math.random() * (6 - 1))
    var random2 = 1 + (Math.random() * (6 - 1))
    var random3 = 1 + (Math.random() * (6 - 1))
    var random4 = 1 + (Math.random() * (6 - 1))

}
